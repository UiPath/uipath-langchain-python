[project]
name = "chat-agent"
version = "0.0.1"
description = "Chat agent with Tavily web search"
authors = [{ name = "John Doe", email = "john.doe@myemail.com" }]
requires-python = ">=3.11"
dependencies = [
    "langgraph>=1.0.10",
    "langchain-anthropic>=1.4.6",
    "tavily-python>=0.7.13",
    "langchain-tavily>=0.2.13",
    "uipath-langchain",
    "uipath",
]

[dependency-groups]
dev = [
     "uipath-dev",
]

